# Server-side (SSR/SSG) fetch base.
# Local dev (recommended): http://localhost:8787  (CHEK dev-gateway)
# Prod/staging: https://api-<env>.chekkk.com
CHEK_API_BASE_URL=

# Optional auth base for proxy identity resolution (fallback: CHEK_API_BASE_URL).
# Recommend setting explicitly in production, e.g. https://api.chekkk.com
CHEK_AUTH_BASE_URL=

# Public site origin used for sitemap/robots.
# Example: https://app-dev.chekkk.com
CHEK_SITE_URL=

# Optional basePath when hosting under a sub-path (e.g. /chek).
# NOTE: Next.js `basePath` is build-time config; `NEXT_PUBLIC_*` also bakes at build time.
CHEK_BASE_PATH=
NEXT_PUBLIC_CHEK_BASE_PATH=

# WeChat OAuth (H5 + PC)
# - AppSecret must stay on backend-auth-saas; frontend only needs AppID(s) to start OAuth redirect.
#
# Two AppIDs are usually required:
# - WeChat browser (H5 oauth2/authorize): MP/Official Account AppID
# - Non-WeChat (PC扫码 qrconnect): Open Platform "Website Application" AppID
#
# Recommended (runtime env read by Next server components):
CHEK_WECHAT_MP_APP_ID=
CHEK_WECHAT_OPEN_APP_ID=
# In WeChat browser: snsapi_base / snsapi_userinfo (default: snsapi_userinfo)
CHEK_WECHAT_SCOPE=
CHEK_AUTH_CLIENT_ID=
CHEK_WECHAT_PACKAGE_NAME=com.chek.app

# Backward compatible single AppID (NOT recommended): used as fallback for both flows.
CHEK_WECHAT_APP_ID=

# Alternative (build-time inlined for client JS):
NEXT_PUBLIC_WECHAT_MP_APP_ID=
NEXT_PUBLIC_WECHAT_OPEN_APP_ID=
NEXT_PUBLIC_WECHAT_SCOPE=
NEXT_PUBLIC_AUTH_CLIENT_ID=
NEXT_PUBLIC_WECHAT_PACKAGE_NAME=com.chek.app

# Backward compatible single AppID (NOT recommended): fallback only.
NEXT_PUBLIC_WECHAT_APP_ID=
